{
  "name": "catbox-fallback",
  "version": "1.0.1",
  "description": "Catbox engine allowing you to use two engines, a primary and a secondary, and if the primary is down, the secondary takes over.",
  "main": "lib/index.js",
  "scripts": {
    "test": "./node_modules/lab/bin/lab -a code -v -L -t 100",
    "test-cov-html": "./node_modules/lab/bin/lab -a code -r html -o coverage.html"
  },
  "keywords": [
    "catbox",
    "fallback",
    "hapi"
  ],
  "author": "Simon Lévesque",
  "license": "MIT",
  "dependencies": {
    "catbox": "^10.0.2",
    "hoek": "^5.0.2"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/tractr/catbox-fallback.git"
  },
  "engines": {
    "node": ">=8.9.0"
  },
  "devDependencies": {
    "catbox-memory": "^3.1.1",
    "catbox-fake": "1.0.0",
    "code": "^5.1.2",
    "lab": "^15.2.1"
  }
}
